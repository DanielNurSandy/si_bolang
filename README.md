git config --global user.email "email yang digunakan di github"

git init

git remote add origin https://github.com/DanielNurSandy/si_bolang.git

git commit -m "pesan bebas"

git push -u origin main / git push origin -u master
